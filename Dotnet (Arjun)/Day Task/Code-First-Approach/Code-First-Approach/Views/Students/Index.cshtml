@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


@* @model IEnumerable<Code_First_Approach.Models.Student> *@

@* <h2>Students</h2> *@
@* <table class="table"> *@
@*     <tr> *@
@*         <th>Name</th> *@
@*         <th>Courses</th> *@
@*     </tr> *@
@*     @foreach (var student in Model) *@
@*     { *@
@*         <tr> *@
@*             <td>@student.Name</td> *@
@*             <td> *@
@*                 @foreach (var sc in student.StudentCourses) *@
@*                 { *@
@*                     <p>@sc.Course.Title</p> *@
@*                 } *@
@*             </td> *@
@*         </tr> *@
@*     } *@
@* </table> *@
@* <a href="/Students/Create">Add Student</a> *@



@model IEnumerable<Code_First_Approach.Models.Student>

<h2>Students</h2>

<a asp-action="Create" class="btn btn-success">Add Student</a>

<table class="table table-bordered mt-3">
    <thead>
        <tr>
            <th>Name</th>
            @* <th>Courses</th> *@
            @* <th>Actions</th> *@
        </tr>
    </thead>
    <tbody>
        @foreach (var student in Model)
        {
            <tr>
                <td>@student.Name</td>
                @* <td> *@
                @*     @foreach (var sc in student.StudentCourses) *@
                @*     { *@
                @*         <p>@sc.Course.Title</p> *@
                @*     } *@
                @* </td> *@
                <td>
                    <a asp-action="Edit" asp-route-id="@student.StudentId" class="btn btn-primary btn-sm">Edit</a>
                    <a asp-action="Delete" asp-route-id="@student.StudentId" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

